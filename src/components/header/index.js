import React from 'react';
import './styles.scss';

const Header = (props) => { 
  return(
    <header>
      <div className="wrap">
        <div className="logo">
          logo
        </div>
      </div>
    </header>
  )
}

export default Header;